---
  - hosts: kube-master
    become_user: root
    become: yes
    roles:
      - sshgen

  - hosts: all 
    become_user: root
    become: yes
    roles:
      - copy_ssh
      - install_deps
      - install_docker

  - hosts: kube-master
    become: yes
    gather_facts: no
    roles:
      - kubekey
    tags: k8s

